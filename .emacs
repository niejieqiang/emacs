(require 'setting-my-crankery)

(require 'setting-smart-tab)

(require 'setting-color-font)

(require 'setting-version-control)

(require 'setting-recentf)

(require 'setting-ibus)

(require 'setting-dired)

(require 'setting-fold)

(require 'setting-cperl-mode)

(require 'setting-auto-complete)

(require 'setting-yasnippet)

(require 'setting-shell-completion)

(require 'setting-pde)

(require 'setting-server)
